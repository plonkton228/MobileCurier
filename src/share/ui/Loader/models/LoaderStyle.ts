import {StyleSheet} from 'react-native'

export const  LoaderStyle = StyleSheet.create({
    LOADMAP: {
     color: "#0000ff"
   },

   LoadContainer: {
    display: "flex",
    flex: 1,
    justifyContent: "center",
    alignItems: "center",

   }

  
})