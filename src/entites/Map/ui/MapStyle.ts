import {StyleSheet, View} from 'react-native';


export const MapStyles = StyleSheet.create({

    mapContainer: {

        width: `100%`,
        height: `100%`,
        zIndex: 1000
    },

    map: {
        height : 10,
        width: 10,
        zIndex: 1000
    }

})

