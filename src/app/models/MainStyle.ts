import { StyleSheet } from "react-native";

export const MainStyle = StyleSheet.create({
    MainContainer: {
        backgroundColor: '#FFFFFF',
        width: '100%',
        height: '100%'
    }
})